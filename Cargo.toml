[workspace]
resolver = "2"
members = ["crates/frontend", "crates/server"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
# panic = "unwind" (default) so panics in one handler 500 instead of killing the process
